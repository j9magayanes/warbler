# PMA Solution

## Getting started

1.  In the `pma-server` folder

    * `npm install`
    * `nodemon` (if installed) or `node index.js`

2.  In the `pma-client` folder

    * `npm install`
    * `npm start`
